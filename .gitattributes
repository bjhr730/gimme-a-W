# Normalize line endings: LF in the repo, native on checkout is disabled so
# editors and CI see the same bytes on Windows, macOS and Linux.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.parquet binary
