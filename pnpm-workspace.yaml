packages:
  - apps/*
  - packages/*

# pnpm 12 refuses dependency install scripts unless listed here.
# esbuild (drizzle-kit, tsx) and sharp (next/image, icon script) need their postinstall.
allowBuilds:
  esbuild: true
  sharp: true
