packages:
  - apps/*
  - packages/*

# pnpm 12 refuses dependency install scripts unless listed here.
# esbuild (pulled in by drizzle-kit and tsx) needs its postinstall to place the binary.
allowBuilds:
  esbuild: true
